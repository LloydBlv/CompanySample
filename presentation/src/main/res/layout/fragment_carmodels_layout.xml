<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/carmodels_fragment_parent"
    android:background="@color/background_super_dark"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >


  <TextView
      android:id="@+id/carmodels_fragment_header_tv"
      android:layout_width="match_parent"
      android:layout_height="?actionBarSize"
      android:textColor="#fff"
      android:textSize="22sp"
      android:padding="8dp"
      android:gravity="center_vertical"
      tools:text="ManufacturerName"
      />

  <android.support.v7.widget.RecyclerView
      android:id="@+id/carmodels_fragment_recyclerview"
      android:paddingTop="?actionBarSize"
      android:clipToPadding="false"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      />


  <ProgressBar
      android:id="@+id/carmodels_fragment_pb"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:indeterminate="true"/>

</android.support.design.widget.CoordinatorLayout>